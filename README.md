# mytvbox
switches.myapp（布尔值 true/false）：这是你这次要用的核心开关。true 放行，false 拦截。
switches.enable_analytics（额外福利）：你可以顺便控制是否开启日志上报。
strings.announcement（额外福利）：如果未来想弹个公告文字，直接改这里，App 读取后显示。
metadata.version（方便排查）：每次修改文件内容后，顺手把这里的版本号或日期改一下，方便你在 App 日志里确认是否拉取到了最新文件。
